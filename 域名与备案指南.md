# 域名与备案指南

> 用自己域名访问作品集，给 HR 看更专业。

---

## 整体流程（3 步）

| 步骤 | 做什么 | 耗时 |
|------|--------|------|
| 1 | 购买域名 | 几分钟 |
| 2 | 备案（ICP） | 1～4 周 |
| 3 | 解析 + 配置 Nginx | 备案通过后，约 30 分钟 |

---

## 第一步：购买域名

### 在阿里云购买（推荐，备案方便）

1. 打开 [阿里云域名注册](https://wanwang.aliyun.com/domain/)
2. 搜索想要的域名（如 `yourname.com`、`yourname.cn`）
3. 加入购物车并支付
4. 价格：`.com` 约 ¥69/年，`.cn` 约 ¥29/年

### 已有域名

如果域名在其他平台（如 GoDaddy、腾讯云），需要：
- 要么**转入阿里云**（方便备案）
- 要么在**原平台做解析**，把域名指到 `39.106.144.152`（备案仍需在阿里云做）

---

## 第二步：备案（ICP）

国内服务器用域名访问**必须备案**。

### 2.1 备案入口

1. 登录 [阿里云控制台](https://www.aliyun.com)
2. 搜索「**备案**」或进入 [备案管理](https://beian.aliyun.com/)
3. 点击「**开始备案**」

### 2.2 准备材料

- **个人**：身份证正反面、手机号、邮箱
- **域名**：需在阿里云购买或已转入
- **备案服务号**：在轻量应用服务器控制台 → 你的服务器详情页 → 「备案」处获取

### 2.3 备案流程

1. 填写主体信息（姓名、证件号等）
2. 填写网站信息（域名、服务器 IP 等）
3. 上传身份证照片
4. 提交初审（阿里云审核，约 1～2 天）
5. 管局审核（各省通信管理局，约 1～3 周）
6. 备案通过后，会收到短信/邮件通知

### 2.4 注意事项

- 备案期间网站可继续用 IP 访问
- 备案主体需与域名实名认证一致
- 不同省份审核时长不同，北京一般较快

---

## 第三步：备案通过后——解析 + Nginx

### 3.1 域名解析

1. 阿里云控制台 → 域名 → 解析设置
2. 添加记录：
   - **记录类型**：A
   - **主机记录**：`@`（代表根域名）或 `www`
   - **记录值**：`39.106.144.152`
   - **TTL**：10 分钟

这样 `你的域名.com` 或 `www.你的域名.com` 会指向你的服务器。

### 3.2 配置 Nginx（用 80 端口，不用 :3000）

在**服务器**上执行：

#### 安装 Nginx

```bash
sudo apt update
sudo apt install nginx -y
sudo systemctl start nginx
sudo systemctl enable nginx
```

#### 创建配置文件

```bash
sudo tee /etc/nginx/conf.d/portfolio.conf << 'EOF'
server {
    listen 80;
    server_name 你的域名.com www.你的域名.com;
    
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
EOF
```

**把 `你的域名.com` 换成你的实际域名。**

#### 检查并重启 Nginx

```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 3.3 访问

在浏览器打开 `http://你的域名.com`，应能正常访问作品集。

---

## 可选：配置 HTTPS（推荐）

让链接变成 `https://`，更安全、更专业。

```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d 你的域名.com -d www.你的域名.com
```

按提示操作，证书会自动配置，并支持自动续期。

---

## 时间线总结

| 时间点 | 你能做的事 |
|--------|------------|
| **现在** | 用 `http://39.106.144.152:3000` 分享给 HR |
| **今天** | 购买域名，提交备案 |
| **1～4 周后** | 备案通过，配置解析 + Nginx，用域名访问 |
| **之后** | 可选配置 HTTPS |

---

## 常见问题

**Q：备案期间网站能访问吗？**  
A：可以，用 IP 访问不受影响。

**Q：域名一定要在阿里云买吗？**  
A：不一定，但阿里云买的域名在阿里云备案最方便。

**Q：备案要钱吗？**  
A：备案免费，域名需要付费。
